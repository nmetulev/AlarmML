<Page
    x:Class="AlarmML.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:AlarmML"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:controls="using:Microsoft.Toolkit.Uwp.UI.Controls"
    xmlns:media="using:Microsoft.Toolkit.Uwp.UI.Media"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid>
        <Grid.Background>
            <LinearGradientBrush StartPoint="0,0" EndPoint="1, 1">
                <LinearGradientBrush.GradientStops>
                    <GradientStop Color="#26d0ce"></GradientStop>
                    <GradientStop Color="#1a2980" Offset="1"></GradientStop>
                </LinearGradientBrush.GradientStops>
            </LinearGradientBrush>
        </Grid.Background>
        
        <TextBlock x:Name="SubText" VerticalAlignment="Bottom" HorizontalAlignment="Center" Margin="40" Foreground="#66FFFFFF" FontSize="30"></TextBlock>

        <Viewbox Margin="50">
            <controls:DropShadowPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                <TextBlock x:Name="TimeText"  TextAlignment="Center" Foreground="#44ff0000" FontFamily="Consolas" FontWeight="Bold" FontSize="400">12:00</TextBlock>
            </controls:DropShadowPanel>
        </Viewbox>

        <InkCanvas x:Name="Inker"></InkCanvas>

        <StackPanel VerticalAlignment="Bottom" HorizontalAlignment="Right" Padding="40">
            <Button Foreground="#66FFFFFF" Click="Button_Click">
                <FontIcon FontFamily="Segoe MDL2 Assets" Glyph="&#xEDAB;" FontSize="40"/>
            </Button>
        </StackPanel>
    </Grid>
</Page>
